#include<iostream>
#include<string>
#include<algorithm>
#define maxn 100000

using namespace std;
long long gcd(long long a,long long b)
{
	if(a%b==0)
		return b;
	else
		gcd(b,a%b);
}
int main()
{
	long long a, b, c;
	cin>>a>>b;
	c = gcd(a,b);
	if(c == 1)
		cout<<"1"<<endl;
	else
	{
		cout<<"1 ";
		if(c != a && c != b)
		{
			cout<<c<<" ";
		}
		for(int i=2; i<=c; i++)
		{

		}
	}
	return 0;
}

