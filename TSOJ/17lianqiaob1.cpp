#include<iostream>
#include<string>
#include<algorithm>

using namespace std;

int main()
{
	string a;
	string b;
	string c="∞Îº€" ;
	while(cin>>a)
	{
		if(a[3]=='’€')
		{
			b+=0.;
			b+=a[0];
			b+=a[1];
		}
		if(a==c)
			b+=0.5;
		for(int i=0; i<b.size(); i++)
		cout<<b[i];
	}
	  
	return 0;
}

