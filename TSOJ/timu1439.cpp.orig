#include<iostream>
#include<string>
#include<algorithm>

using namespace std;

int main()
{
	int v, n;
	while(cin>>v)
	{
		cin>>n;
		int vi[40], va[40];
		int maxm;
		for(int i=0; i<n; ++i)
		{
			cin>>vi[i]>>va[i];
		}
		for(int i=0; i<n; i++)
		{
			for(int i=0; i<n; i++){
			
			if(v>=vi[i])
			{
				v-=vi[i];
				max+=va[i];
			}}
			
		}
	}
	return 0;
}

