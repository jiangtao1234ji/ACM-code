#include<iostream>
#include<string>
#include<algorithm>

using namespace std;

int main()
{
	int n, m;
	int x;
	int a[100000], b[100000];
	while(cin>>n>>m)
	{
		int k=0
		for(int i=0; i<n; ++i)
			for(int j=0; j<m; ++j)
			{
				cin>>x;
				if(x==1)
				{
					a[k++]=i;
					b[k++]=j;
				}
			}
		for(int i=0; i<k; ++i)
		{
			if(i<n-1)
						cout<<"("<<i+1<<","<<j+1<<")"<<">";
					else
						cout<<"("<<i+1<<","<<j+1<<")"<<endl;
		}
	}
	return 0;
}

