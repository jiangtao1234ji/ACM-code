#include<iostream>
#include<cstring>
#include<algorithm>

using namespace std;

int main()
{
	int n;
	long long ans=1;
	while(cin>>n)
	{
		int m[n];
		m[0]=1;
		m[1]=2;
		if(n==1)
			cout<<m[0]<<endl;
		else if(n==2)
			cout<<m[1]<<endl;
		else{
		
		for(int i=2; i<n; ++i)
		{
			
		}}
	}
	return 0;
}

